services:
  - type: web
    name: animation-auto-engine
    env: node
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg
      npm install
    startCommand: node server.js
